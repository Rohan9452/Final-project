import 'package:flutter/material.dart';

class SeminarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Seminar'),
      ),
      body: Center(
        child: Text('Details about Seminar'),
      ),
    );
  }
}
